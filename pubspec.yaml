name: barcode_folder_renamer
description: App para renomear pastas com código de barras
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  barcode_scan2: ^4.2.0
  path_provider: ^2.1.1
  path: ^1.8.3

flutter:
  uses-material-design: true
